#ifndef MAINWINDOW_H
#define MAINWINDOW_H

#include <QMainWindow>
#include "../common/qtincludes.h"

class MainWindow : public QMainWindow
{
    Q_OBJECT

public:
    explicit MainWindow();
    ~MainWindow();
public slots:

private:
//    QSerialPort *OdinSerial;

private slots:

};

#endif // MAINWINDOW_H
